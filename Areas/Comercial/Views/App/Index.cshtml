@* @model IEnumerable<AppServiceAndTravel.Models.Cotizacion>
@{
    ViewData["Title"] = "Cotizaciones";
    ViewData["Breadcrumb"] = "Historial y gestión de cotizaciones";

    string BadgeEstado(AppServiceAndTravel.Models.EstadoCotizacion e) => e switch {
        AppServiceAndTravel.Models.EstadoCotizacion.Pendiente  => "badge-pendiente",
        AppServiceAndTravel.Models.EstadoCotizacion.Aprobada   => "badge-aprobada",
        AppServiceAndTravel.Models.EstadoCotizacion.Rechazada  => "badge-rechazada",
        _ => "bg-secondary"
    };

    string IconoEstado(AppServiceAndTravel.Models.EstadoCotizacion e) => e switch {
        AppServiceAndTravel.Models.EstadoCotizacion.Pendiente  => "bi-clock",
        AppServiceAndTravel.Models.EstadoCotizacion.Aprobada   => "bi-check-circle-fill",
        AppServiceAndTravel.Models.EstadoCotizacion.Rechazada  => "bi-x-circle-fill",
        _ => "bi-question-circle"
    };
}
 *@
<!-- Stats Row -->
<div class="row g-3 mb-4">
    <div class="col-sm-6 col-xl-3">
        <div class="card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-circle d-flex align-items-center justify-content-center"
                     style="width:48px;height:48px;background:#fef3c7;color:#92400e;font-size:22px;">
                    <i class="bi bi-clock"></i>
                </div>
                <div>
                    <div class="text-muted" style="font-size:12px;">Pendientes</div>
                    <div style="font-size:24px;font-weight:700;color:#92400e;">TotalPendientes</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-circle d-flex align-items-center justify-content-center"
                     style="width:48px;height:48px;background:#d1fae5;color:#065f46;font-size:22px;">
                    <i class="bi bi-check-circle-fill"></i>
                </div>
                <div>
                    <div class="text-muted" style="font-size:12px;">Aprobadas</div>
                    <div style="font-size:24px;font-weight:700;color:#065f46;">TotalAprobadas</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-circle d-flex align-items-center justify-content-center"
                     style="width:48px;height:48px;background:#fee2e2;color:#991b1b;font-size:22px;">
                    <i class="bi bi-x-circle-fill"></i>
                </div>
                <div>
                    <div class="text-muted" style="font-size:12px;">Rechazadas</div>
                    <div style="font-size:24px;font-weight:700;color:#991b1b;">TotalRechazadas</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-circle d-flex align-items-center justify-content-center"
                     style="width:48px;height:48px;background:#dbeafe;color:#1e40af;font-size:22px;">
                    <i class="bi bi-file-earmark-text"></i>
                </div>
                <div>
                    <div class="text-muted" style="font-size:12px;">Total</div>
                    <div style="font-size:24px;font-weight:700;color:#1e40af;">TotalRegistros</div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Filtros y tabla -->
<div class="card">
    <div class="card-header bg-white py-3 d-flex flex-wrap align-items-center gap-3">
        <span class="fw-semibold flex-grow-1">Historial de Cotizaciones</span>

        <!-- Filtro estado -->
        <div class="btn-group btn-group-sm" Rol="group">
            <a href="@Url.Action("Index")" class="btn btn-dark">Todas</a>
            <a href="@Url.Action("Index", new { estado = AppServiceAndTravel.Models.EstadoCotizacion.Pendiente })"
               class="btn btn-warning">Pendientes</a>
            <a href="@Url.Action("Index", new { estado = AppServiceAndTravel.Models.EstadoCotizacion.Aprobada })"
               class="btn btn-success">Aprobadas</a>
            <a href="@Url.Action("Index", new { estado = AppServiceAndTravel.Models.EstadoCotizacion.Rechazada })"
               class="btn btn-danger">Rechazadas</a>
        </div>

        <!-- Búsqueda -->
        <form method="get" class="d-flex gap-2">
@*             @if (ViewBag.EstadoFiltro != null)
            {
               
            } *@
            <input type="hidden" name="estado" value="EstadoFiltro" />
            <input type="search" name="busqueda" value="Busqueda"
                   class="form-control form-control-sm" placeholder="Buscar cliente, ruta…" style="min-width:200px;" />
            <button type="submit" class="btn btn-sm btn-outline-primary"><i class="bi bi-search"></i></button>
        </form>

@*         @if (User.IsInRole("Administrador") || User.IsInRole("Coordinador") || User.IsInRole("Operador"))
        {

        } *@
        <a asp-action="Create" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-lg me-1"></i>Nueva
        </a>
    </div>

    <div class="card-body p-0">
@*         @if (Model == null || !Model.Any())
        {

        }
        else
        {
           @foreach (var c in Model)
                    {
                        <tr>
                            <td class="ps-4 fw-semibold text-muted">#@c.Id.ToString("D6")</td>
                            <td>
                                <div class="fw-semibold">@c.Cliente?.NombreCompleto</div>
                                <small class="text-muted">@c.Cliente?.correo</small>
                            </td>
                            <td>
                                <small>
                                    <span class="text-muted">@c.Origen</span>
                                    <i class="bi bi-arrow-right mx-1 text-muted"></i>
                                    <span class="text-muted">@c.Destino</span>
                                </small>
                            </td>
                            <td>@c.FechaServicioRequerido.ToString("dd/MM/yyyy")</td>
                            <td class="fw-semibold">$@c.ValorCotizado.ToString("N0")</td>
                            <td>
                                <span class="badge @BadgeEstado(c.Estado) px-3 py-2 rounded-pill">
                                    <i class="bi @IconoEstado(c.Estado) me-1"></i>@c.Estado
                                </span>
                            </td>
                            <td><small class="text-muted">@c.FechaCreacion.ToString("dd/MM/yy")</small></td>
                            <td class="text-end pe-4">
                                <div class="d-flex justify-content-end gap-1">
                                    <a asp-action="Details" asp-route-id="@c.Id"
                                       class="btn btn-sm btn-outline-secondary" title="Ver detalles">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    @if ((User.IsInRole("Administrador") || User.IsInRole("Coordinador")) &&
                                                                    c.Estado == AppServiceAndTravel.Models.EstadoCotizacion.Pendiente)
                                    {
                                        <a asp-action="Edit" asp-route-id="@c.Id"
                                           class="btn btn-sm btn-outline-primary" title="Editar">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                    }
                                    @if ((User.IsInRole("Administrador") || User.IsInRole("Aprobador")) &&
                                                                    c.Estado == AppServiceAndTravel.Models.EstadoCotizacion.Pendiente)
                                    {
                                        <button type="button" class="btn btn-sm btn-outline-success"
                                                title="Aprobar"
                                                data-bs-toggle="modal"
                                                data-bs-target="#modalAprobar"
                                                data-id="@c.Id"
                                                data-valor="@c.ValorCotizado">
                                            <i class="bi bi-check-lg"></i>
                                        </button>
                                        <button type="button" class="btn btn-sm btn-outline-danger"
                                                title="Rechazar"
                                                data-bs-toggle="modal"
                                                data-bs-target="#modalRechazar"
                                                data-id="@c.Id">
                                            <i class="bi bi-x-lg"></i>
                                        </button>
                                    }
                                    @if (c.Estado == AppServiceAndTravel.Models.EstadoCotizacion.Aprobada &&
                                                                    c.Servicio == null &&
                                                                    (User.IsInRole("Administrador") || User.IsInRole("Coordinador")))
                                    {
                                        <a asp-controller="Servicio" asp-action="Create" asp-route-cotizacionId="@c.Id"
                                           class="btn btn-sm btn-primary" title="Generar Servicio">
                                            <i class="bi bi-truck me-1"></i>Servicio
                                        </a>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
        } *@
        <div class="text-center py-5 text-muted">
            <i class="bi bi-inbox" style="font-size:40px;opacity:.3;"></i>
            <p class="mt-2 mb-0">No hay cotizaciones que mostrar.</p>
        </div>
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th class="ps-4">N°</th>
                        <th>Cliente</th>
                        <th>Ruta</th>
                        <th>Fecha Servicio</th>
                        <th>Valor</th>
                        <th>Estado</th>
                        <th>Creada</th>
                        <th class="text-end pe-4">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    
                </tbody>
            </table>
        </div>
    </div>

    <!-- Paginación -->
@*     @if ((int)ViewBag.TotalPaginas > 1)
    {
        
    } *@
    <div class="card-footer bg-white d-flex align-items-center justify-content-between py-3">
        <small class="text-muted">
            Mostrando de TotalRegistros registros
        </small>
        <nav>
            <ul class="pagination pagination-sm mb-0">
                <li class="page-item disabled">
                    <a class="page-link" href="@Url.Action("Index", new { pagina = (int)ViewBag.PaginaActual - 1, estado = ViewBag.EstadoFiltro, busqueda = ViewBag.Busqueda })">‹</a>
                </li>
 @*                @for (int p = 1; p <= (int)ViewBag.TotalPaginas; p++)
                {

                } *@
                <li class="page-item activo">
                    <a class="page-link"></a>
                </li>
                <li class="page-item disabled">
                    <a class="page-link">›</a>
                </li>
            </ul>
        </nav>
    </div>
</div>

<!-- Modal Aprobar -->
<div class="modal fade" id="modalAprobar" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <form asp-action="Aprobar" method="post">
                @* @Html.AntiForgeryToken() *@
                <input type="hidden" id="aprobarId" name="id" />
                <div class="modal-header border-0 pb-0">
                    <h5 class="modal-title"><i class="bi bi-check-circle text-success me-2"></i>Aprobar Cotización</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Valor Aprobado</label>
                        <div class="input-group">
                            <span class="input-group-text">$</span>
                            <input type="number" id="aprobarValor" name="valorAprobado" class="form-control"
                                   step="0.01" min="0" placeholder="Dejar vacío para usar valor cotizado" />
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Observaciones</label>
                        <textarea name="observaciones" class="form-control" rows="3"
                                  placeholder="Observaciones sobre la aprobación…"></textarea>
                    </div>
                </div>
                <div class="modal-footer border-0 pt-0">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-success"><i class="bi bi-check-lg me-1"></i>Aprobar</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal Rechazar -->
<div class="modal fade" id="modalRechazar" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <form asp-action="Rechazar" method="post">
                @* @Html.AntiForgeryToken() *@
                <input type="hidden" id="rechazarId" name="id" />
                <div class="modal-header border-0 pb-0">
                    <h5 class="modal-title"><i class="bi bi-x-circle text-danger me-2"></i>Rechazar Cotización</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Motivo del rechazo <span class="text-danger">*</span></label>
                        <textarea name="motivo" class="form-control" rows="3" required
                                  placeholder="Explique por qué se rechaza la cotización…"></textarea>
                    </div>
                </div>
                <div class="modal-footer border-0 pt-0">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-danger"><i class="bi bi-x-lg me-1"></i>Rechazar</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
document.getElementById('modalAprobar').addEventListener('show.bs.modal', function(e) {
    var btn = e.relatedTarget;
    document.getElementById('aprobarId').value = btn.dataset.id;
    document.getElementById('aprobarValor').value = btn.dataset.valor;
});
document.getElementById('modalRechazar').addEventListener('show.bs.modal', function(e) {
    document.getElementById('rechazarId').value = e.relatedTarget.dataset.id;
});
</script>
}
